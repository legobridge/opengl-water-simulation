#include "mesh.h"

namespace shape
{
	// Get a Mesh object that comprises a cuboid
	Mesh getCuboidalMesh(float x, float y, float z);

	// Get a Mesh object that comprises a right cylinder
	Mesh getCylindricalMesh(float r, float h);
}